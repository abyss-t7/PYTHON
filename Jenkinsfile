pipeline {
    agent any
    stages {
        stage('Hello') {
            steps {
                echo 'Jenkins is alive'
                echo 'Just suck the github'
                sh 'docker --version'
                sh 'whoami'
            }
        }
    }
}
